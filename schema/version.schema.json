{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "App version manifest",
  "type": "object",
  "additionalProperties": false,
  "required": ["versionCode", "versionName", "updateUrl", "sha256", "updateNotes", "isForceUpdate"],
  "properties": {
    "versionCode": {
      "type": "integer",
      "minimum": 1
    },
    "versionName": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+-(tv|mobile)$"
    },
    "updateUrl": {
      "type": "string",
      "format": "uri",
      "pattern": "^https://github\\.com/IO-Player/app-updates/releases/download/.+\\.apk$"
    },
    "sha256": {
      "type": "string",
      "description": "SHA-256 digest of the APK at updateUrl, lowercase hex. The app must verify the downloaded file against this before installing it.",
      "pattern": "^[a-f0-9]{64}$"
    },
    "updateNotes": {
      "type": "string",
      "minLength": 1
    },
    "isForceUpdate": {
      "type": "boolean"
    }
  }
}
